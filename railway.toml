[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Worker process — no HTTP port to health-check.
# Use the Dockerfile entrypoint so codex login runs before the runner starts.
startCommand = "/app/entrypoint.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
